import React, { Component } from 'react';
import routerData from '../router/routerData';

class CommonLayout extends Component {
  render() {

    // const router = renderRou
    return (
      <>
        routerdata 테스트
      </>
    );
  }
}

export default CommonLayout;
// export default connect(
//   ({ auth }) => ({
//     logged: auth.logged,
//     user: auth.user,
//     type: auth.user.type
//   }),
//   null
// )(CommonLayout);